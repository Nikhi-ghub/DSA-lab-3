#include <stdio.h>

int main() {
    int array[25];
    int i, sum = 0;

    for (i = 0; i < 25; i++) {
        array[i] = i + 1; 
    }

    for (i = 0; i < 25; i++) {
        sum = sum + array[i];
    }

    printf("The sum of numbers from 1 to 25 is: %d\n", sum);

    return 0;
}
